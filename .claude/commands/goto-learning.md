---
description: Jump to a specific feature by its number
argument-hint: <feature number 1-54>
---
Jump to feature number **$ARGUMENTS** in the Claude course, per `CLAUDE.md`.

Set that feature as the current feature in `learning/_progress.md` (add it to the active plan if it
isn't already there), then begin its 3-step teaching loop (one demo per selected interface that supports it), showing the progress line. If **$ARGUMENTS**
is empty or not a valid feature number (1–54), list the 54 features from `claude-code-key-features.md`
and ask me which one I want.
