---
description: Show course progress (where am I)
---
Show my course status per `CLAUDE.md`: print the progress line, then a recap of the **selected interfaces**, the features
**completed / current / remaining** within the active plan (with per-interface demo status), plus the demo folders created so far. If
`learning/_progress.md` does not exist yet, tell me the course hasn't started and suggest `/start-learning`.
